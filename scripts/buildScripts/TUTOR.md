# How to use build script

There's 3 options:

1. Use .sh script

```bash
chmod +x build.sh
./build.sh
```

2. Use .bat script

```powershell
build.bat
```

3. Use python script (Universal), but to use it you need to install Python 3.x

```bash
python build.py
```
